#include <stdio.h>

/**
 * main - Prints a complex string followed by a new line
 * Return: Always 0(success)
 * on error, -1 is returned
*/

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return(0);
}
